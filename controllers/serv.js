// const Msg = require('../models/servModel');

// const newMsg = async (req, res) => {
//   const { chatMessage, nickname } = req.body;
//   await Msg.add(chatMessage, nickname);
//   res.render('success');
// };