class SessionStore {
  findSession(_id) {
    this.findSession();
  }

  saveSession(_id, _session) {
    this.saveSession();
  }

  deleteSession(_id) {
    this.deleteSession();
  }

  findAllSessions() {
    this.findAllSessions();
  }
}

module.exports = SessionStore;
